# Scratch / local build artefacts
.tmp/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual environment
.venv/
.venv-*/
venv/

# IDE
.idea/
.vscode/
*.swp

# Data — all of these may contain patient PII, never commit
*.db
data/
output/
docs/
chroma_db/

# Local model caches (Whisper, HF transformers, etc.) — large, machine-local
models/

# Scratch / local eval indexes (e.g. SapBERT recall eval)
.tmp/

# Environment
.env
setup.sh

# Scratch / gitignored work area (validation runs, temp ChromaDB, etc.)
.tmp/

# OS
.DS_Store
Thumbs.db

# Microsoft Office lock files
~$*

# Scratch (ChromaDB rebuilds, eval indexes)
.tmp/
